<div class="card">
    <div class="card-header">
        <h1>{{ post.title }}</h1>
        <p>Posted by {{user.username}} on {{ created_at }}</p>
    </div>

    <p class="cardContent">{{ content }}</p>
</div>